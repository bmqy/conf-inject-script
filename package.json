{
  "name": "conf-inject-script",
  "version": "1.0.0",
  "description": "基于Cloudflare Worker的conf配置文件注入脚本",
  "main": "src/index.js",
  "scripts": {
    "test": "node test/test.js",
    "generate:wrangler": "node scripts/generate-wrangler.js",
    "config:generate": "npm run generate:wrangler",
    "deploy": "npm run generate:wrangler && npx wrangler deploy",
    "dev": "npm run generate:wrangler && npx wrangler dev",
    "kv:create": "npx wrangler kv namespace create CONFIG_KV",
    "kv:list": "npx wrangler kv namespace list",
    "logs": "npx wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "quantumultx",
    "loon",
    "conf",
    "inject"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "toml": "^3.0.0"
  }
}
